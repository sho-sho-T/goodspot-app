import { PlaygroundPageTemplate } from '@/features/playground/components/server/PlaygroundPageTemplate'

const PlaygroundPage = (_props: PageProps<'/playground'>) => {
  return <PlaygroundPageTemplate />
}

export default PlaygroundPage
